spring:
  cloud:
    nacos:
      config:
        enabled: false

send:
  sms:
    active: Ali
  email:
    active: Apache

upload:
  active: Local
  local:
    base-path: xx
  oss:
    bucket: xx
    endpoint: xx
    access-key-id: xx
    secret-access-key: xx